{
  "entities": {
    "ContactFormSubmission": {
      "title": "Contact Form Submission",
      "type": "object",
      "description": "A submission from the website contact form.",
      "properties": {
        "fullName": {
          "type": "string",
          "description": "The full name of the person."
        },
        "email": {
          "type": "string",
          "format": "email",
          "description": "The email address of the person."
        },
        "contactNumber": {
          "type": "string",
          "description": "The contact number of the person."
        },
        "message": {
          "type": "string",
          "description": "The message from the person."
        },
        "submissionDate": {
          "type": "string",
          "format": "date-time",
          "description": "The date and time of the submission."
        }
      },
      "required": ["fullName", "email", "message", "submissionDate"]
    }
  },
  "auth": {
    "providers": []
  },
  "firestore": {
    "/contactFormSubmissions/{submissionId}": {
      "schema": {
        "$ref": "#/entities/ContactFormSubmission"
      },
      "description": "Stores all submissions from the contact form."
    }
  }
}
